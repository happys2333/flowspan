# Evidence: hosted CI foundation, 2026-07-13

Classification: **Hosted CI**

Branch: `codex/v1-foundation`

Verified source commits:

- `f6bcaf35f564316615e92c1bfdca614a802a0f71` (platform safety contracts);
- `adb181117b030dacdf080174f66c4bf6b818c792` (signed discovery primitives);
- `e464194ff5deae95cc7c18467be77ab9abc7e8c5` (authenticated TCP control);
- `e11e7e884698e557bb6c24471442e54bd850ff46` (identity-store port);
- `71d25c752db54a268cedbce46ea3f5da002b5915` (identity payload codec);
- `43e981b270823342b914a0ad19c84b97299cb6b2` (Windows DPAPI identity store);
- `c3a5aa2cd3893edf798fae99ef6339e069b5e7fc` (macOS Keychain identity store);
- `38f6eab9e881ccc83f9457626c01bc6a52152972` (Linux Secret Service identity
  store);
- `08e47897fc56e59049c5cb787bfbb0bc9647ce41` (trust-revocation session
  shutdown);
- `92d9c0d09a56d176c040dca7a8d4c4720b88bc32` (atomic persistent trust
  authority);
- `aea492ede3fd8abf071a3e75766115b13cb94858` (macOS Keychain trust store);
- `fd22524a46d3abde8dbbb10c4bbeaab15bfcb4bf` (Windows DPAPI trust store);
- `8b3e11b4ab7b56bd64241f52a1774d9f99a09605` (Linux Secret Service trust
  store);
- `2ef99614c83f78bac84f9cb00a3d9f287913ff62` (authenticated reconnect
  supervision);
- `fc39d6e1dd2e59fbd999ac18ce2d2117ec8cdf5d` (trusted authenticated
  reconnect composition);
- `6bf191ba164f50c726d9927a897c5d880c7d1a7d` (immutable Node 24 CI Action
  pins);
- `a3771ef6453a2d3700461001acf37e677af9e9a5` (trusted DNS-SD browser and
  candidate source);
- `5dee727087f4bdd5baac5593449c33fbfd05aa1b` (signed DNS-SD offer publisher);
- `657c9dd0e2eff854ae1e094e450ab207da976ca5` (authenticated multi-peer
  inbound listener);
- `79dae126e25d7b0472340dbe9159c3a0b8326c15` (bounded pairing ceremony);
- `8e2a410334f44ebcde8dbf3b552c48ee95074f41` (unified pairing/session
  listener);
- `3439e2d3e367a113f96f1af96216da4f535ced46` (accessible desktop composition
  root);
- `592ebc088c0285f7af36b118082a6258e79db68c` (desktop pairing confirmation
  bridge).

## Foundation GitHub Actions results

CI run [29227691213](https://github.com/happys2333/flowspan/actions/runs/29227691213)
completed successfully for the verified commit. The following jobs and all of
their required steps reported `success`:

- `Test (windows-latest)` (`86745169782`): checkout, pinned SDK install,
  locked restore, formatting verification, warnings-as-errors build, tests,
  deterministic simulator, and evidence upload;
- `Test (macos-latest)` (`86745169773`): the same checks;
- `Test (ubuntu-latest)` (`86745169797`): the same checks;
- `Secret scan` (`86745169786`): committed-content scan.

CodeQL run
[29227691222](https://github.com/happys2333/flowspan/actions/runs/29227691222)
also completed successfully for the same commit. Its `Analyze C#` job
(`86745169871`) restored locked dependencies, built the analyzed source, and
completed CodeQL analysis successfully.

The runs were created at `2026-07-13T05:59:43Z` and completed by
`2026-07-13T06:01:30Z`. Run IDs, commit SHA, job names, step conclusions, and
timestamps were read using `gh run view` on 2026-07-13.

## Discovery-slice GitHub Actions results

CI run [29228398597](https://github.com/happys2333/flowspan/actions/runs/29228398597)
completed successfully for `adb1811`. All required steps reported `success` in:

- `Test (windows-latest)` (`86747222988`);
- `Test (macos-latest)` (`86747223008`);
- `Test (ubuntu-latest)` (`86747222979`);
- `Secret scan` (`86747222950`).

Each OS job checked out the source, installed the pinned SDK, restored locked
dependencies, verified formatting, built with warnings as errors, ran the test
suites and deterministic simulator, and uploaded test evidence.

CodeQL run
[29228398586](https://github.com/happys2333/flowspan/actions/runs/29228398586)
also completed successfully for `adb1811`; its `Analyze C#` job
(`86747222890`) restored, built, and analyzed the committed source. These runs
were created at `2026-07-13T06:14:29Z` and completed by
`2026-07-13T06:16:38Z`.

## Authenticated-TCP GitHub Actions results

CI run [29230360522](https://github.com/happys2333/flowspan/actions/runs/29230360522)
completed successfully for `e464194`. All required steps reported `success` in:

- `Test (windows-latest)` (`86753052134`);
- `Test (macos-latest)` (`86753052074`);
- `Test (ubuntu-latest)` (`86753052069`);
- `Secret scan` (`86753052079`).

CodeQL run
[29230360589](https://github.com/happys2333/flowspan/actions/runs/29230360589)
also completed successfully; its `Analyze C#` job (`86753052275`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T06:54:34Z` and completed by `2026-07-13T06:56:43Z`.

## Identity-store-port GitHub Actions results

CI run [29231259122](https://github.com/happys2333/flowspan/actions/runs/29231259122)
completed successfully for `e11e7e8`. All required steps reported `success` in:

- `Test (windows-latest)` (`86755788675`);
- `Test (macos-latest)` (`86755788695`);
- `Test (ubuntu-latest)` (`86755788679`);
- `Secret scan` (`86755788629`).

CodeQL run
[29231258915](https://github.com/happys2333/flowspan/actions/runs/29231258915)
also completed successfully; its `Analyze C#` job (`86755787770`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T07:11:35Z` and completed by `2026-07-13T07:13:40Z`.

## Identity-payload-codec GitHub Actions results

CI run [29231679641](https://github.com/happys2333/flowspan/actions/runs/29231679641)
completed successfully for `71d25c7`. All required steps reported `success` in:

- `Test (windows-latest)` (`86757090967`);
- `Test (macos-latest)` (`86757091018`);
- `Test (ubuntu-latest)` (`86757090985`);
- `Secret scan` (`86757090981`).

CodeQL run
[29231679637](https://github.com/happys2333/flowspan/actions/runs/29231679637)
also completed successfully; its `Analyze C#` job (`86757090902`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T07:19:02Z` and completed by `2026-07-13T07:21:25Z`.

## Windows-DPAPI GitHub Actions results

CI run [29232607800](https://github.com/happys2333/flowspan/actions/runs/29232607800)
completed successfully for `43e981b`. All required steps reported `success` in:

- `Test (windows-latest)` (`86759951760`);
- `Test (macos-latest)` (`86759951789`);
- `Test (ubuntu-latest)` (`86759951745`);
- `Secret scan` (`86759951709`).

CodeQL run
[29232607790](https://github.com/happys2333/flowspan/actions/runs/29232607790)
also completed successfully; its `Analyze C#` job (`86759951660`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T07:36:16Z` and completed by `2026-07-13T07:38:31Z`.

## macOS-Keychain GitHub Actions results

CI run [29233261468](https://github.com/happys2333/flowspan/actions/runs/29233261468)
completed successfully for `c3a5aa2`. All required steps reported `success` in:

- `Test (windows-latest)` (`86762018382`);
- `Test (macos-latest)` (`86762018387`);
- `Test (ubuntu-latest)` (`86762018320`);
- `Secret scan` (`86762018326`).

CodeQL run
[29233261486](https://github.com/happys2333/flowspan/actions/runs/29233261486)
also completed successfully; its `Analyze C#` job (`86762018689`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T07:48:19Z` and completed by `2026-07-13T07:49:58Z`.

## Linux-Secret-Service GitHub Actions results

CI run [29235097127](https://github.com/happys2333/flowspan/actions/runs/29235097127)
completed successfully for `38f6eab`. All required steps reported `success` in:

- `Test (ubuntu-latest)` (`86767834571`);
- `Test (windows-latest)` (`86767834587`);
- `Test (macos-latest)` (`86767834715`);
- `Secret scan` (`86767834565`).

The Ubuntu job exercised the production process boundary's structured recovery
when `secret-tool` is absent, 4 KiB stdout/stderr limits, and caller-cancellation
process-tree termination. These are process contracts and do not constitute a
live desktop Secret Service round trip.

CodeQL run
[29235097119](https://github.com/happys2333/flowspan/actions/runs/29235097119)
also completed successfully; its `Analyze C#` job (`86767834583`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T08:20:31Z` and completed by `2026-07-13T08:23:02Z`.

## Trust-revocation GitHub Actions results

CI run [29235903833](https://github.com/happys2333/flowspan/actions/runs/29235903833)
completed successfully for `08e4789`. All required steps reported `success` in:

- `Test (windows-latest)` (`86770378078`);
- `Test (macos-latest)` (`86770378088`);
- `Test (ubuntu-latest)` (`86770378081`);
- `Secret scan` (`86770378056`).

CodeQL run
[29235903801](https://github.com/happys2333/flowspan/actions/runs/29235903801)
also completed successfully; its `Analyze C#` job (`86770377796`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T08:34:18Z`; CI completed at `2026-07-13T08:35:47Z`, and CodeQL
completed at `2026-07-13T08:36:35Z`.

## Persistent-trust GitHub Actions results

CI run [29237639814](https://github.com/happys2333/flowspan/actions/runs/29237639814)
completed successfully for `92d9c0d`. All required steps reported `success` in:

- `Test (windows-latest)` (`86775951165`);
- `Test (macos-latest)` (`86775951061`);
- `Test (ubuntu-latest)` (`86775951049`);
- `Secret scan` (`86775950971`).

CodeQL run
[29237639674](https://github.com/happys2333/flowspan/actions/runs/29237639674)
also completed successfully; its `Analyze C#` job (`86775949780`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T09:03:41Z`; CI completed at `2026-07-13T09:05:23Z`, and CodeQL
completed at `2026-07-13T09:06:14Z`.

## macOS-Keychain-trust GitHub Actions results

CI run [29238440463](https://github.com/happys2333/flowspan/actions/runs/29238440463)
completed successfully for `aea492e`. All required steps reported `success` in:

- `Test (windows-latest)` (`86778508324`);
- `Test (macos-latest)` (`86778508341`);
- `Test (ubuntu-latest)` (`86778508298`);
- `Secret scan` (`86778508302`).

The macOS job executed disposable Security.framework trust
create/restart/update/revoke in addition to the update/add race contracts.
Windows and Ubuntu exercised the fake boundary and explicit platform rejection.

CodeQL run
[29238440487](https://github.com/happys2333/flowspan/actions/runs/29238440487)
also completed successfully; its `Analyze C#` job (`86778508361`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T09:16:13Z`; CI completed at `2026-07-13T09:18:08Z`, and CodeQL
completed at `2026-07-13T09:18:33Z`.

## Windows-DPAPI-trust GitHub Actions results

CI run [29238845804](https://github.com/happys2333/flowspan/actions/runs/29238845804)
completed successfully for `fd22524`. All required steps reported `success` in:

- `Test (windows-latest)` (`86779804652`);
- `Test (macos-latest)` (`86779804757`);
- `Test (ubuntu-latest)` (`86779804656`);
- `Secret scan` (`86779804530`).

The Windows job executed CurrentUser DPAPI trust create/restart/update/revoke and
proved that identity-protected data cannot be unprotected under the distinct
trust entropy/context. All runners exercised atomic-file replace, cancellation,
and old-snapshot preservation through the fake protector contract.

CodeQL run
[29238845707](https://github.com/happys2333/flowspan/actions/runs/29238845707)
also completed successfully; its `Analyze C#` job (`86779803847`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T09:22:26Z`; CI completed at `2026-07-13T09:24:39Z`, and CodeQL
completed at `2026-07-13T09:24:50Z`.

## Linux-Secret-Service-trust GitHub Actions results

CI run [29239901273](https://github.com/happys2333/flowspan/actions/runs/29239901273)
completed successfully for `8b3e11b`. All required steps reported `success` in:

- `Test (ubuntu-latest)` (`86783177157`);
- `Test (macos-latest)` (`86783177171`);
- `Test (windows-latest)` (`86783177183`);
- `Secret scan` (`86783177276`).

The Ubuntu job exercised the production `secret-tool` process boundary with a
trust lookup larger than the identity-store 4 KiB limit but within the bounded
trust envelope, rejected output beyond the trust envelope, and retained the
missing-tool and cancellation/process-tree contracts. These tests do not use a
live, unlocked desktop Secret Service.

CodeQL run
[29239901353](https://github.com/happys2333/flowspan/actions/runs/29239901353)
also completed successfully; its `Analyze C#` job (`86783177492`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T09:39:04Z`; CI completed at `2026-07-13T09:40:34Z`, and CodeQL
completed at `2026-07-13T09:41:43Z`.

## Reconnect-supervision GitHub Actions results

CI run [29242129544](https://github.com/happys2333/flowspan/actions/runs/29242129544)
completed successfully for `2ef9961`. All required steps reported `success` in:

- `Test (macos-latest)` (`86790394933`);
- `Test (windows-latest)` (`86790394939`);
- `Test (ubuntu-latest)` (`86790394955`);
- `Secret scan` (`86790394901`).

Each hosted OS restored locked dependencies, verified formatting, built with
warnings as errors, ran 192 tests and the deterministic simulator, and uploaded
test evidence. The tests exercise serialized retry, bounded backoff reset,
network-change cancellation/burst coalescing, caller-cancellation draining,
permanent-rejection race priority, cancellation-callback fault containment, and
the BCL subscription lifecycle through deterministic boundaries. They do not
cause a physical network-interface change.

CodeQL run
[29242129527](https://github.com/happys2333/flowspan/actions/runs/29242129527)
also completed successfully; its `Analyze C#` job (`86790395291`) restored,
built, and analyzed the committed source. The runs were created at
`2026-07-13T10:15:40Z`; CI completed at `2026-07-13T10:17:29Z`, and CodeQL
completed at `2026-07-13T10:18:00Z`.

## Authenticated-reconnect GitHub Actions results

CI run [29244620266](https://github.com/happys2333/flowspan/actions/runs/29244620266)
completed successfully for full commit
`fc39d6e1dd2e59fbd999ac18ce2d2117ec8cdf5d`. All required steps reported
`success` in:

- `Test (macos-latest)` (`86798445044`);
- `Test (ubuntu-latest)` (`86798445082`);
- `Test (windows-latest)` (`86798445102`);
- `Secret scan` (`86798445016`).

Each hosted OS restored locked dependencies, verified formatting, built with
warnings as errors, ran all 207 tests and the deterministic simulator, and
uploaded test evidence. The tests include a real loopback authenticated TCP
session plus deterministic contracts for fresh trust/capability preflight,
candidate-key binding, handshake outcome classification, post-handshake trust
registration, revoke/downgrade draining, and the revoke-before-registration
race. A loopback socket does not prove physical LAN or interface behavior.

CodeQL run
[29244620236](https://github.com/happys2333/flowspan/actions/runs/29244620236)
also completed successfully; its `Analyze C#` job (`86798444697`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-13T10:58:46Z`; CI completed at `2026-07-13T11:00:19Z`, and CodeQL
completed at `2026-07-13T11:01:26Z`. Run IDs, full commit SHA, job and step
conclusions, test logs, and timestamps were queried with `gh run view` on
2026-07-13.

## Node-24 Action-pin GitHub Actions results

CI run [29245431042](https://github.com/happys2333/flowspan/actions/runs/29245431042)
completed successfully for full commit
`6bf191ba164f50c726d9927a897c5d880c7d1a7d`. It executed only immutable Action
commit references whose reviewed releases declare the Node 24 runtime. All
required steps reported `success` in:

- `Test (ubuntu-latest)` (`86801056620`);
- `Test (windows-latest)` (`86801056629`);
- `Test (macos-latest)` (`86801056630`);
- `Secret scan` (`86801056635`).

This includes checkout, pinned SDK installation, locked restore, formatting,
warnings-as-errors build, all tests, simulator, artifact upload, and the
upgraded gitleaks action. CodeQL run
[29245431263](https://github.com/happys2333/flowspan/actions/runs/29245431263)
also completed successfully using CodeQL Action `v4.37.0`; its `Analyze C#` job
(`86801057602`) restored, built, and analyzed the committed source.

Both runs were created at `2026-07-13T11:12:08Z`. CI completed at
`2026-07-13T11:13:54Z`, and CodeQL completed at `2026-07-13T11:14:45Z`. Run and
job conclusions, full commit SHA, steps, and timestamps were queried with
`gh run view` on 2026-07-13.

## DNS-SD-browser GitHub Actions results

CI run [29247250092](https://github.com/happys2333/flowspan/actions/runs/29247250092)
completed successfully for full commit
`a3771ef6453a2d3700461001acf37e677af9e9a5`. All required steps reported
`success` in:

- `Test (ubuntu-latest)` (`86807021713`);
- `Test (macos-latest)` (`86807021720`);
- `Test (windows-latest)` (`86807021759`);
- `Secret scan` (`86807021744`).

Each hosted OS restored all 22 locked projects, verified formatting, built with
warnings as errors, ran all 226 tests and the deterministic simulator, and
uploaded test evidence. The new suites contributed 51 transport tests and 9
isolated mDNS-adapter tests. Hosted execution compiled and exercised package
record translation and injected lifecycle boundaries; it did not start the
default Makaretu stack or exchange a multicast packet.

CodeQL run
[29247250079](https://github.com/happys2333/flowspan/actions/runs/29247250079)
also completed successfully; its `Analyze C#` job (`86807021447`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-13T11:43:38Z`; CI completed at `2026-07-13T11:45:36Z`, and CodeQL
completed at `2026-07-13T11:45:53Z`. Run/job/step conclusions, full commit SHA,
test counts, and timestamps were queried with `gh run view` on 2026-07-13.

## DNS-SD-publisher GitHub Actions results

CI run [29248555224](https://github.com/happys2333/flowspan/actions/runs/29248555224)
completed successfully for full commit
`5dee727087f4bdd5baac5593449c33fbfd05aa1b`. All required steps reported
`success` in:

- `Test (ubuntu-latest)` (`86811275969`);
- `Test (macos-latest)` (`86811275999`);
- `Test (windows-latest)` (`86811275973`);
- `Secret scan` (`86811275953`).

Each hosted OS restored all 22 locked projects, verified formatting, built with
warnings as errors, ran all 236 tests and the deterministic simulator, and
uploaded test evidence. The suites include 56 transport tests and 14 isolated
mDNS-adapter tests. They verify immediate signed-offer publication, timed fresh
nonce refresh, withdrawal, stack-replacement replay, failed-publish rollback,
and startup/cleanup fault preservation through deterministic boundaries. They
do not prove that a multicast announcement crossed a physical interface.

CodeQL run
[29248555219](https://github.com/happys2333/flowspan/actions/runs/29248555219)
also completed successfully; its `Analyze C#` job (`86811275883`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-13T12:05:44Z`; CI completed at `2026-07-13T12:07:32Z`, and CodeQL
completed at `2026-07-13T12:08:27Z`. Run/job/step conclusions, full commit SHA,
test counts, and timestamps were queried with `gh run view` on 2026-07-13.

## Authenticated-inbound-listener GitHub Actions results

CI run [29250862560](https://github.com/happys2333/flowspan/actions/runs/29250862560)
completed successfully for full commit
`657c9dd0e2eff854ae1e094e450ab207da976ca5`. All required steps reported
`success` in:

- `Test (ubuntu-latest)` (`86818771464`);
- `Test (macos-latest)` (`86818771454`);
- `Test (windows-latest)` (`86818771438`);
- `Secret scan` (`86818771427`).

Each hosted OS restored all 22 locked projects, verified formatting, built with
warnings as errors, ran all 246 tests and the deterministic simulator, and
uploaded test evidence. The suites include 50 security tests, 65 transport
tests, and all 9 authenticated-inbound-listener tests. Those tests exercise two
different current trust records through one real loopback port, unknown-peer
recovery, claimed-ID/key binding, capability denial, slot backpressure, handler
failure isolation, current-key recheck, and revoke/cancel/fatal-accept draining.

CodeQL run
[29250862610](https://github.com/happys2333/flowspan/actions/runs/29250862610)
also completed successfully; its `Analyze C#` job (`86818771541`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-13T12:42:20Z`; CI completed at `2026-07-13T12:43:55Z`, and CodeQL
completed at `2026-07-13T12:44:44Z`. Run/job/step conclusions, full commit SHA,
test counts, and timestamps were queried with `gh run view` on 2026-07-13.
The hosted listeners used same-process loopback sockets; no job accepted a peer
through a physical interface or validated firewall/multicast behavior.

## Pairing-ceremony GitHub Actions results

CI run [29257451887](https://github.com/happys2333/flowspan/actions/runs/29257451887)
completed successfully for full commit
`79dae126e25d7b0472340dbe9159c3a0b8326c15`. All required steps reported
`success` in:

- `Test (macos-latest)` (`86841445636`);
- `Test (ubuntu-latest)` (`86841445832`);
- `Test (windows-latest)` (`86841445846`);
- `Secret scan` (`86841445725`).

Each hosted OS restored all 22 locked projects, verified formatting, built with
warnings as errors, ran all 268 tests and the deterministic simulator, and
uploaded test evidence. The suites include 71 security tests and 66 transport
tests, covering the bounded canonical pairing wire, deterministic hostile input,
transcript and completion proof, rejection/deadline/tamper/identity/persistence
failures, and a complete two-store ceremony over one real loopback TCP
connection.

CodeQL run
[29257451849](https://github.com/happys2333/flowspan/actions/runs/29257451849)
also completed successfully; its `Analyze C#` job (`86841445916`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-13T14:19:27Z`; CI completed at `2026-07-13T14:21:21Z`, and CodeQL
completed at `2026-07-13T14:22:16Z`. Run/job/step conclusions, full commit SHA,
test counts, and timestamps were queried with `gh run view` on 2026-07-13. The
pairing transport remained same-process loopback; the jobs did not exercise a
desktop confirmation UI, human SAS comparison, physical LAN, firewall path, or
production-listener pairing/session multiplexing.

## Unified-listener GitHub Actions results

CI run [29260042166](https://github.com/happys2333/flowspan/actions/runs/29260042166)
completed successfully for full commit
`8e2a410334f44ebcde8dbf3b552c48ee95074f41`. All required steps reported
`success` in:

- `Test (macos-latest)` (`86850531126`);
- `Test (ubuntu-latest)` (`86850531152`);
- `Test (windows-latest)` (`86850531246`);
- `Secret scan` (`86850531118`).

Each hosted OS restored all 22 locked projects, verified formatting, built with
warnings as errors, ran all 287 tests and the deterministic simulator, and
uploaded test evidence. The suites include 82 security tests and 74 transport
tests. They cover strict `FSP1`/`FSH1` initial-family selection, one-owner
prefetched-frame routing, same-port pairing followed by a new authenticated
connection, independent pairing/session capacity, injected selection timeout,
listener cancellation/fatal-accept drain, and pairing-registration/revocation
serialization through one Trust coordinator.

CodeQL run
[29260042125](https://github.com/happys2333/flowspan/actions/runs/29260042125)
also completed successfully; its `Analyze C#` job (`86850533021`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-13T14:55:47Z`; CI completed at `2026-07-13T14:57:57Z`, and CodeQL
completed at `2026-07-13T14:58:26Z`. Run/job/step conclusions, full commit SHA,
test counts, and timestamps were queried with `gh run view` on 2026-07-13. All
network integration remained same-process loopback; no job exercised a human
prompt, physical LAN/firewall path, or discovery-to-connect flow.

## Desktop-composition GitHub Actions results

CI run [29296981647](https://github.com/happys2333/flowspan/actions/runs/29296981647)
completed successfully for full commit
`3439e2d3e367a113f96f1af96216da4f535ced46`. All required steps reported
`success` in:

- `Test (windows-latest)` (`86972560577`);
- `Test (ubuntu-latest)` (`86972560594`);
- `Test (macos-latest)` (`86972560608`);
- `Secret scan` (`86972560580`).

Each hosted OS restored all 24 locked projects, verified formatting, built with
warnings as errors, ran all 295 tests with 0 failures and 0 skips, ran the
explicit TEST MODE desktop composition validator, ran the deterministic
simulator, and uploaded test evidence. The suites include 8 desktop headless
tests; the macOS job also completed all 10 platform-macOS tests, including its
disposable production-Keychain paths.

CodeQL run
[29296981677](https://github.com/happys2333/flowspan/actions/runs/29296981677)
also completed successfully; its `Analyze C#` job (`86972560596`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-14T00:49:56Z`; CI completed at `2026-07-14T00:52:09Z`, and CodeQL
completed at `2026-07-14T00:52:56Z`. Run/job/step conclusions, full commit SHA,
test counts, composition-validator output, and timestamps were queried with
`gh run view` on 2026-07-14. The validator intentionally used an in-memory
identity and did not create a native window; hosted success is not native UI or
screen-reader evidence.

## Desktop-pairing-confirmation GitHub Actions results

CI run [29298480369](https://github.com/happys2333/flowspan/actions/runs/29298480369)
completed successfully for full commit
`592ebc088c0285f7af36b118082a6258e79db68c`. All required steps reported
`success` in:

- `Test (ubuntu-latest)` (`86977046510`);
- `Test (macos-latest)` (`86977046523`);
- `Test (windows-latest)` (`86977046537`);
- `Secret scan` (`86977046546`).

Each hosted OS restored all 24 locked projects, verified formatting, built with
warnings as errors, ran all 306 tests with 0 failures and 0 skips, ran the
explicit TEST MODE desktop composition validator, ran the deterministic
simulator, and uploaded test evidence. Each desktop suite reported 19 passes,
including full two-node accept/reject ceremonies over same-process loopback TCP.

CodeQL run
[29298480397](https://github.com/happys2333/flowspan/actions/runs/29298480397)
also completed successfully; its `Analyze C#` job (`86977046600`) restored,
built, and analyzed the committed source. Both runs were created at
`2026-07-14T01:24:42Z`; CI completed at `2026-07-14T01:26:50Z`, and CodeQL
completed at `2026-07-14T01:27:26Z`. Run/job/step conclusions, full commit SHA,
test counts, composition-validator output, and timestamps were queried with
`gh run view` on 2026-07-14. The desktop pairing adapter was injected by tests;
the production shell still did not start a listener or discover a physical peer.

## What this proves

- Commit `f6bcaf3` checks out with LF-normalized source on GitHub-hosted Windows,
  macOS, and Ubuntu runners.
- Commits through `592ebc0` restore locked packages, pass repository formatting,
  compile with warnings treated as errors, run the committed test suites and
  simulator, and upload test evidence on all three hosted OS images.
- The signed discovery, expiry/deduplication, identity-change, and reconnect
  backoff primitives committed in `adb1811` pass on all three hosted OS images.
- The authenticated ephemeral handshake, bounded direct TCP control channel,
  identity/version binding, hostile frame limits, and handshake timeout committed
  in `e464194` pass on all three hosted OS images.
- The identity-store port, atomic provisioning behavior, deletion/restart
  lifecycle, concurrent first-launch convergence, and explicit degraded-store
  classification committed in `e11e7e8` pass on all three hosted OS images.
- The bounded/versioned identity payload codec, canonical round trip, hostile
  input rejection, and PKCS#8 validation committed in `71d25c7` pass on all
  three hosted OS images.
- The Windows atomic-file contract passes on all three images. The Windows job
  also executes a disposable CurrentUser DPAPI create/reload/delete smoke; the
  macOS and Ubuntu jobs verify that the production protector rejects use outside
  Windows rather than degrading.
- The macOS Keychain contracts pass on all images. The macOS job also executes
  disposable Security.framework create/reload/delete and concurrent `SecItemAdd`
  smoke tests; Windows and Ubuntu verify explicit platform rejection.
- The Linux `secret-tool` contracts pass on all images. Ubuntu additionally
  executes missing-tool recovery, bounded-output, cancellation, and process-tree
  termination tests; Windows and macOS verify explicit platform rejection.
- Trust revocation, capability-downgrade session selection, concurrent admission
  rejection, and stop-failure fan-out pass on all three hosted OS images.
- The canonical bounded trust codec, golden fixture, persistent authority
  restart/failure/concurrency contracts, durable revoke ordering, and local
  shutdown draining pass on all three hosted OS images through a protected-store
  contract fake.
- The macOS job additionally persists trust through a disposable
  `WhenUnlockedThisDeviceOnly` Keychain item and exercises atomic
  `SecItemUpdate`, create-race recovery, restart, capability update, and
  revocation. This is hosted user-profile evidence, not signed-package or
  locked-Keychain evidence.
- The Windows job persists trust through CurrentUser DPAPI with a separate trust
  context, fsynced same-directory atomic replacement, restart, capability
  update, and revocation. This is hosted user-profile evidence, not a signed
  package, service account, locked profile, or physical-machine result.
- The Linux trust adapter uses a separately attributed `kind=trust` item,
  cross-process replacement coordination, stdin-only secret transport, and a
  trust-specific bounded output envelope. Ubuntu proves those process contracts,
  not Secret Service persistence or desktop collection behavior.
- The authenticated reconnect supervisor serializes attempts, drains cancellation
  before retry, resets bounded backoff after network change or an authenticated
  session, coalesces change bursts, and lets permanent rejection outrank a
  concurrent availability event. The production BCL change-source adapter
  subscribes and disposes on all hosted OSes; no job changed a real interface.
- A verified discovery candidate is bound to its signed address, port, and peer
  key before connection. Every attempt re-reads trust and capability, performs
  an authenticated loopback TCP handshake, and registers the live session with
  the trust coordinator. Revocation and capability downgrade cancel and drain
  the handler, including the handshake-to-registration race. These are
  loopback/contract results, not production DNS-SD or physical-LAN evidence.
- The committed history passed the configured secret scan, and the C# source
  passed the configured CodeQL workflow.
- The workflow's third-party Actions execute successfully from the reviewed,
  immutable Node 24 commit pins recorded in the dependency inventory.
- The canonical bounded DNS-SD TXT payload, current-trust candidate source,
  dual-stack endpoint rotation/scope handling, unsafe-address rejection, and
  expiry/removal behavior pass on all three hosted OS images.
- The isolated provisional Makaretu adapter compiles on all three hosted OS
  images. Its split-record cache, record/package translation, bounds, full-stack
  network-change replacement, and injected bind/restart/subscriber failures pass
  through deterministic fakes; no job opened a production multicast session.
- The signed-offer publisher's immediate publication, fresh-nonce refresh,
  withdrawal, restart replay, and combined lifecycle/cleanup fault reporting pass
  on all three hosted OS images. Profile construction reaches the third-party
  DNS-SD boundary, but no hosted assertion observes an emitted multicast packet.
- A shared authenticated listener routes different claimed Device IDs only to
  current trust, verifies the selected trusted identity/key, rechecks trust
  before capability registration, bounds active handshake/session slots, and
  isolates peer or handler failure on all three hosted OS images. Revocation,
  caller cancellation, and fatal accept failure drain the expected loopback
  sessions.
- The bounded pairing ceremony negotiates the highest common version, verifies
  transcript possession before prompting, exchanges signed dual confirmation
  and distinct completion proofs before local Trust registration, and preserves
  deterministic reject/deadline/tamper/identity/persistence/cleanup behavior on
  all three hosted OS images. The direct-TCP evidence remains loopback and uses
  decision-source test doubles rather than people or a desktop UI.
- One published listener classifies only pairing or authenticated hello
  envelopes, transfers the pre-read frame to one decoder, closes the pairing
  socket, and requires a new authenticated connection before handling control.
  Its pairing/session capacity, selection timeout, Trust mutation ordering,
  cancellation, and fatal-accept behavior pass on all three hosted OS images.
- The desktop composition root, protected-identity startup boundary, failure
  redaction/retry/cancellation behavior, truthful non-sharing/empty states,
  declared automation names, and keyboard identity disclosure pass their
  headless and explicit TEST MODE contracts on all three hosted OS images.
- The desktop pairing decision bridge starts with zero Capabilities, requires
  explicit code-comparison acknowledgement, enforces one visible prompt, rejects
  disposal, and protects cancellation/stale/out-of-order prompt transitions on
  all three hosted OS images. Two loopback ceremonies prove both-side acceptance
  and asymmetric local grants, while one rejection leaves both Trust Stores empty.

## What this does not prove

- A live, unlocked Linux desktop Secret Service create/reload/delete round trip,
  locked collection behavior, desktop prompts, or packaged application context.
- DPAPI behavior in locked profiles, Windows services, roaming profiles, signed
  packages, or physical-machine installation contexts.
- Keychain ACL/prompt behavior in signed/notarized packages, locked login
  Keychains, or migrated user profiles.
- Physical-device LAN discovery, multi-interface/VPN behavior, sleep/wake, or
  IPv4/IPv6 interoperability.
- Native permission, capture, protected-surface, remote-input, accessibility,
  packaging, signing, install, update, or long-duration behavior on real Windows,
  macOS, or Linux machines.
- A native desktop window launch, visual rendering, or screen-reader interaction;
  the hosted composition validator intentionally does not create a window.
- Production desktop discovery/listener composition, a human two-device SAS
  comparison, or persistent Trust written by the desktop through a platform
  credential store; the pairing UI tests inject the verified decision port.
- Absence of vulnerabilities or secrets beyond the configured scanners' scope.

Those claims remain open in `docs/release/v1-release-criteria.md`.
